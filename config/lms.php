<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Reports — fallback значения для отчётов LMS
    |--------------------------------------------------------------------------
    |
    | reports.default_deadline — «фолбэк-фолбэк» для расчёта соблюдения сроков.
    | Используется, если у задания отсутствует `LmsAssignment::deadline` и у
    | события не задан `LmsEvent::default_assignment_deadline`. Также служит
    | дедлайном для тестов (`lms_tests`), у которых нет собственного поля
    | дедлайна. Источник правды по продукту — поле события
    | `lms_events.default_assignment_deadline`; данный ключ — последняя
    | подстраховка. Хранится в ISO-8601 с UTC-суффиксом `…Z`, как и сериализация
    | дат в моделях LMS (см. `LmsEvent::serializeDate`).
    |
    */
    'reports' => [
        'default_deadline' => env('LMS_REPORTS_DEFAULT_DEADLINE', '2026-06-20T23:59:59Z'),
    ],

];
