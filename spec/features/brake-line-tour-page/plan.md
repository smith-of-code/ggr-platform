# План: brake-line-tour-page

## Milestones

1. Добавить `whitespace-pre-line` к `day.description` в `resources/js/Pages/Tours/Show.vue`
2. Визуальная проверка на странице `/tours/zelenyy-atom`

## Верификация

По паттерну Docker-команд из spec-continuation (source docker/.env.local && docker exec ...).
