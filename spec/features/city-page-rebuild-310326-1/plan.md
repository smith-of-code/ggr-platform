# План: city-page-rebuild-310326-1

## Milestones

### Фаза 1 (выполнена)
1. **Миграция + модель** — добавить JSON колонку `energy_cities_block`, accessor в City
2. **Бэкенд-валидация** — расширить `store`/`update` в Admin\CityController
3. **Админ-форма** — секция «Энергия городов» в Form.vue с полями блока
4. **Публичная страница** — секция на Show.vue по прототипу (два столбца, тёмный фон)
5. **Верификация** — линтер, визуальная проверка

### Фаза 2: переключатели видимости блоков
6. **Миграция** — JSON колонка `block_visibility` в таблице `cities`
7. **Модель + контроллер** — fillable, cast, accessor с дефолтами true; валидация
8. **Админ-форма** — RCheckbox переключатели для каждого блока в правой колонке
9. **Публичная страница** — проверка visibility перед рендером каждого блока
10. **Верификация** — линтер, build

## UI Components

- `RichTextEditor` — для поля описания в админке
- `RInput` — для текстовых полей (video URL, заголовок, текст кнопки, URL кнопки)
- `RCard` — обёртка секции в админ-форме
- `RCheckbox` — переключатели видимости блоков (из `@rosatom-ggr/ui-kit`, глобально зарегистрирован)
- Существующий `videoEmbedSrc` computed — рефакторинг в утилиту для переиспользования

## Verification

Согласно Command Execution Pattern из spec-continuation.
