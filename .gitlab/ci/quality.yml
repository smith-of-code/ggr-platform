pint:
  extends:
    - .php_base
    - .quality_rules
  stage: quality
  script:
    - vendor/bin/pint --test

frontend:lint:
  extends:
    - .node_base
    - .quality_rules
  stage: quality
  script:
    - npm run format:check
    - npm run lint
